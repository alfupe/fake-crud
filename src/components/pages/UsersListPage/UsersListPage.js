import React from 'react';
import { withAdminLayout } from '../../HOC/WithLayout';

const UsersListPage = props => {
    return (
        <section>users list page</section>
    );
};

export default withAdminLayout(UsersListPage);
